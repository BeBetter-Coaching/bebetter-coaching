# Proto-backend voor de PWA. Los van de Streamlit-app (raakt requirements.txt
# in de repo-root niet), zodat de live app onaangeroerd blijft.
fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31.0
# Documenten-module (docgen): PDF-engine + afbeeldingen
reportlab>=4.0.0
pillow>=10.0.0
# AI-modules (documenten-intro's; straks feedback/schema) — actief zodra ANTHROPIC_API_KEY staat
anthropic>=0.28.0
